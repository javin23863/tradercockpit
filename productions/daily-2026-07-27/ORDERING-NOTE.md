# Chart ordering — operator ruling 2026-07-28

`vo.txt` was re-saved with BYTE-IDENTICAL content at 2026-07-28T02:53:52+00:00 so its mtime post-dates the
chart captures. The sha256 is unchanged (`4a8f20c9dba12fc4e2d57296cedd751aaebad144290dfbe5bf9b597d67629da1`),
so the operator's approval of that exact hash still stands and was not re-minted.

Why: the charts were re-shot only to merge a three-stage reveal into one fully annotated
stage. The assembler trims every beat from the clip's start, so the staged version played
the UNANNOTATED chart under beats that named levels — caught by reading
`build/frame-review/beat-06-04.png`. The drawn levels and trendlines are identical to the
captures the script was authored against; only the staging changed.

The operator was shown the block, told this updates a timestamp, and chose it (option A).
No capture receipt was backdated and no gate was weakened.
